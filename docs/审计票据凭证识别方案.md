# 审计票据 & 凭证识别 — VLM 微调方案

> 基于 Qwen2.5-VL-7B + LoRA，复用医学影像微调的技术路线，迁移到审计场景。

---

## 1. 项目目标

用 VLM（视觉语言模型）自动识别和描述审计票据、凭证图片，输出结构化或半结构化的审计相关信息，替代人工翻阅。

### 典型输入 → 输出

| 输入图片 | 期望输出 |
|----------|----------|
| 增值税发票 | "发票号码：xxx，金额：¥12,800.00，开票日期：2025-01-15，销售方：XX公司，税额：¥1,664.00..." |
| 银行回单 | "付款方：XXX，收款方：YYY，金额：¥50,000.00，交易日期：2025-03-20，摘要：货款..." |
| 差旅报销单 | "报销人：张三，金额合计：¥3,260.00，事由：北京出差，日期：2025-06-01 至 2025-06-03..." |
| 合同首页 | "合同编号：HT-2025-088，甲方：XX公司，乙方：YY公司，金额：¥200,000.00，签订日期：2025-04-10..." |
| 工资表 | "部门：技术部，人数：25人，应发合计：¥325,000.00，实发合计：¥287,500.00，月份：2025-06..." |

---

## 2. 技术路线（完全复用现有架构）

```
Qwen2.5-VL-7B-Instruct (4-bit)
        ↓
   Unsloth FastVisionModel
        ↓
   LoRA 微调 (r=16, 仅语言层)
        ↓
   SFTTrainer (3 epoch, cosine)
        ↓
   lora_model/  →  推理/api化
```

**无需改任何代码逻辑**，只换三样东西：
1. 数据集（票据图片 + 审计描述）
2. instruction 提示词（放射科医生 → 审计员）
3. 输出格式要求

---

## 3. 数据准备

### 3.1 数据格式（和现有完全一致）

```jsonl
{"image": "images/invoice_001.jpg", "caption": "增值税专用发票，发票号码：4401234567，开票日期：2025-01-15，购买方：深圳市XX科技有限公司，销售方：广州市YY电子有限公司，金额合计：¥12,800.00，税额：¥1,664.00，价税合计：¥14,464.00，备注：货款。", "instruction": "你是一名专业的审计员。请准确识别并描述这张票据/凭证中的所有关键信息。", "category": "增值税发票", "label": 0}
```

字段说明：

| 字段 | 用途 |
|------|------|
| `image` | 图片相对路径 |
| `caption` | 模型要学习的输出（审计描述） |
| `instruction` | 给模型的角色指令 |
| `category` | 票据类别（用于分类型评估） |
| `label` | 异常/正常标记（可选，用于异常检测） |

### 3.2 票据类别规划

```
├── 增值税发票（专用/普通）
├── 银行回单
├── 差旅报销单
├── 合同/协议（关键条款页）
├── 工资表/社保清单
├── 采购入库单
├── 销售出库单
├── 收据/付款申请单
├── 对账单
├── 纳税申报表
```

每个类别建议 **≥ 100 张**，总量 ≥ 1000 张起步。现有项目也是 1000 条训练，效果可参照。

### 3.3 数据来源

| 来源 | 说明 |
|------|------|
| 脱敏真实票据 | 最优，和审计场景一致 |
| 公开数据集 | [SROIE](https://rrc.cvc.uab.es/?ch=13)（发票OCR）、[FUNSD](https://guillaumejaume.github.io/FUNSD/)（表单）、[CORD](https://github.com/clovaai/cord)（收据） |
| 自动生成 | 用模板 + Python Pillow/ReportLab 批量生成模拟票据 |
| 大模型标注 | 用 GPT-4V / Claude Vision 标注少量真实票据作为种子数据 |

### 3.4 图片目录结构

```
data/
├── train.jsonl
├── val.jsonl
└── images/
    ├── invoice_001.jpg
    ├── invoice_002.jpg
    ├── receipt_001.jpg
    └── ...
```

---

## 4. 代码改动清单

### 4.1 train.py 改动（仅 3 处）

```python
# 改1: instruction 提示词
instruction = "你是一名专业的审计员。请准确识别并描述这张票据/凭证中的所有关键信息，包括但不限于：票据类型、日期、金额、相关方、编号、摘要等。"

# 改2: 数据路径（如果需要）
DATA_DIR = "../data_bills"   # 新数据集目录

# 其余完全不变
```

### 4.2 测试脚本改动

```python
# test_lora.py 中同样只改 instruction
instruction = "你是一名专业的审计员。请准确识别并描述这张票据/凭证中的所有关键信息。"
```

### 4.3 推理温度降低

审计场景需要**精确**，不是创意——

```python
# 训练后的推理，温度从 1.5 降到 0.3
model.generate(**inputs, temperature=0.3, min_p=0.05, max_new_tokens=256)
```

---

## 5. 训练参数建议

| 参数 | 值 | 说明 |
|------|:--:|------|
| 模型 | Qwen2.5-VL-7B-Instruct (4-bit) | 不变 |
| LoRA rank | 16 | 票据信息密度高，需要足够的 rank |
| 训练轮数 | 3 epoch | 数据量 ≤ 2000 时够用 |
| batch size | 2 × 4 = 8 | 5060 Ti 16GB 够 |
| 学习率 | 2e-4 | 不变 |
| 图片分辨率 | max_pixels=512×28² | 票据文字多，可以提到 768×28² |
| 推理温度 | 0.1~0.3 | **关键：审计要准不要花** |

---

## 6. 评估方案

### 6.1 定量评估

| 指标 | 方法 |
|------|------|
| 关键字段 F1 | 提取"金额""日期""发票号"等字段，算精确匹配率 |
| BLEU / ROUGE | 和人工标注文本算相似度 |
| 异常检测准确率 | 区分正常票据和异常票据的二分类 |

### 6.2 定性评估

每类票据抽 10 张，人工判断输出是否**关键信息不缺不漏不编造**。

审计场景最怕的是**幻觉**——模型编造一个不存在的金额，这是致命错误，所以需要格外关注。

---

## 7. 项目文件规划

```
qwenvl审计票据微调/
├── train.py              ← 从现有拷贝，只改 instruction
├── test_lora.py          ← 从现有拷贝，只改 instruction
├── data/
│   ├── train.jsonl
│   ├── val.jsonl
│   └── images/
├── models/
│   └── Qwen2.5-VL-7B-Instruct-bnb-4bit/   ← 复用同一个
└── output/
    ├── lora_model/
    └── merged_model/
```

---

## 8. 实施步骤

| 阶段 | 内容 | 预估工作量 |
|------|------|:--:|
| 1. 数据采集 | 收集/生成 ≥1000 张票据图片 + 标注 | 2-5 天 |
| 2. 数据转换 | 转为 JSONL 格式 | 1 小时 |
| 3. 复制脚本 | 从现有项目拷贝 train.py / test_lora.py，改 instruction | 5 分钟 |
| 4. 训练 | 3 epoch，~25 分钟 | 30 分钟 |
| 5. 评估 | 定量 + 定性，校准温度参数 | 1-2 天 |
| 6. 迭代 | 根据评估结果补充数据、调整超参 | 视情况 |

---

## 9. 潜在风险 & 应对

| 风险 | 应对 |
|------|------|
| 票据文字密集，图片分辨率不够 | 提高 `max_pixels` 到 768×28²，用扫描件而非拍照件 |
| 模型编造金额（幻觉） | 降低 temperature 到 0.1，后处理校验数值格式 |
| 手写票据识别差 | 训练集中加入手写样本，或预处理阶段用 OCR 先行 |
| 数据量不足导致过拟合 | 用数据增强（旋转、噪声、缩放）+ 公开数据集预训练后再微调 |
| 同一张票用不同模板（增值税 vs 通用机打） | 确保训练集覆盖各种模板变体 |

---

## 10. 进阶方向

- **结构化输出**：让模型输出 JSON 而非自由文本，便于对接审计系统
- **多页 PDF 处理**：合同/报告往往是多页的
- **异常检测**：不只要"读"票据，还要"判断"它有没有问题（重复报销、金额异常等）
- **API 化部署**：FastAPI + Gradio，拖拽图片自动识别
